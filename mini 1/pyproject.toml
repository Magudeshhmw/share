[project]
name = "stock-forecaster"
version = "0.1.0"
description = "A Flask application for stock price forecasting."
requires-python = ">=3.9"
dependencies = [
    "flask",
    "yfinance",
    "pandas",
    "numpy",
    "matplotlib",
    "statsmodels",
    "gunicorn",
]
